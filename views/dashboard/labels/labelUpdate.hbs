<h1>update a label</h1>
{{!-- action : WHERE SHOULD THE DATA GO ? --}}
{{!-- IT MUST BE LINKED TO A ROUTE --}}
{{!-- this way is the legacy way : the synchronous method --}}
<form method="post" action="/dashboard/label/update/{{label._id}}" enctype="multipart/form-data">
    {{!-- NAMES attributes ARE MANDATORY --}}

    <p>
        <label class="form-label" for="logo">logo</label>
        <input type="file" class="form-control" name="logo" value="{{label.logo}}">
    </p>
    <p>
        <label class="form-label" for="">name</label>
        <input name="name" class="form-control" type="text" value="{{label.name}}">
    </p>
    <p>
        <label class="form-label" for="">country</label>
        <input name="country" class="form-control" type="text" value="{{label.country}}">
    </p>
    <p>
        <label class="form-label" for="">city</label>
        <input name="city" class="form-control" type="text" value="{{label.city}}">
    </p>
    <p>
        <label class="form-label" for="">street</label>
        <input name="street" class="form-control" type="text" value="{{label.street}}">
    </p>
    <p>
        <label class="form-label" for="">street num</label>
        <input name="streetNumber" class="form-control" type="text" value="{{label.streetNumber}}">
    </p>

    <p>
        <label class="form-label" for="">zip</label>
        <input name="zipcode" class="form-control" type="number" value="{{label.zipcode}}">
    </p>

    <button>ok</button>
</form>